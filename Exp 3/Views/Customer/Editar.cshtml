@model Exp_3.Models.CLIENTES

@{
    ViewBag.Title = "Editar";
}

<h2>Editar</h2>


@using (Html.BeginForm("Modificar", "Customer", FormMethod.Post, new { @class = "formhorizontal", role = "form" }))

{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>CLIENTES</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.codigo)

        @Html.HiddenFor(model => model.nombre)

        @Html.HiddenFor(model => model.direccion)

        @Html.HiddenFor(model => model.telefono)

        @Html.HiddenFor(model => model.email)

        @Html.HiddenFor(model => model.edad)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
